# Warlords MARL — Local Requirements
# Only needed for running the tournament notebook locally
# Training is done in Google Colab
# Install with: pip install -r Requirements.txt

# Stable-Baselines3 (for loading the trained PPO model in agent1.py)
stable-baselines3>=2.3.0

# Multi-agent environment (tournament notebook)
pettingzoo[atari]>=1.24.0
supersuit>=3.9.0

# Numerical
numpy>=1.26.0

# Video recording (tournament notebook)
imageio>=2.34.0
imageio-ffmpeg>=0.4.9